{
    "buildCommand": "npm --prefix frontend/CarCulator run build",
    "outputDirectory": "frontend/CarCulator/dist",
    "framework": "vite",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}